package converter.note;

import converter.note.Note;

public class GuitarNote extends Note {
    public GuitarNote(String line, String name, int distanceFromMeasureStart, int position) {
        super(line, name, distanceFromMeasureStart, position);
    }
}
